name: Update Submodule
on: 
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v1
        with:
          repository: StaveService/b
          token: ${{ secrets.STAVE_ACCESS_TOKEN }}
          event-type: front_update

